<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Button Width="200" Height="80" Margin="10" Content="{Binding Name}" Click="Button_Click"></Button>
            <Button Width="200" Height="80" Margin="10" Content="{Binding Name}" Command="{Binding ChangeName}" Click="Button_Click"></Button>
            <Button Width="200" Name="btnTestResources" Height="80" Margin="10" Content="测试Resources" Command="{Binding ChangeName}" Click="BtnTestResources_Click"></Button>
        </StackPanel>
    </Grid>
</Window>
